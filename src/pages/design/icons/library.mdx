---
title: Icon Library
---

import { graphql } from 'gatsby';

### Tokens

Icon tokens represent specific concepts in Zendesk products.
Before adding an icon to your project, check the list below to see if the
concept has an icon token associated with it.

import { TokenSearch } from '../../../examples/design/icons/TokenSearch';

<TokenSearch data={props.data.tokens} icons={props.data.svgIcons.edges} />

### All Icons

This is a list of all the icons in the Garden library, independent of context.
Use these icons only if you can’t find a suitable token.

import { SvgSearch } from '../../../examples/design/icons/SvgSearch';

<SvgSearch data={props.data.svgIcons} />

export const pageQuery = graphql`
  query ($fileAbsolutePath: String) {
    ...SidebarPageFragment
    svgIcons: allFile(
      filter: {
        sourceInstanceName: { eq: "svg-icons" }
        relativeDirectory: { eq: "16" }
        name: { glob: "!wordmark-*" }
      }
      sort: { fields: name, order: ASC }
    ) {
      totalCount
      edges {
        node {
          name
          relativeDirectory
          childGardenSvg {
            content
          }
        }
      }
    }
    tokens: allIconToken {
      totalCount
      edges {
        node {
          style
          synonyms
          icon
          id
        }
      }
    }
  }
`;
