---
title: Toggle
description: >
  A Toggle lets users turn something on and off like a light switch. Unlike a Checkbox, which is
  used for selection, a Toggle is used for activation.
package: '@zendeskgarden/react-forms'
components:
  - forms/src/elements/common/Field.tsx
  - forms/src/elements/common/Hint.tsx
  - forms/src/elements/common/Label.tsx
  - forms/src/elements/common/Message.tsx
  - forms/src/elements/Toggle.tsx
---

<Usage>
  <Use>
    <ul>
      <li>To see or compare the results of a settings change</li>
      <li>To activate a mode (such as "dark mode") which takes immediate effect</li>
    </ul>
  </Use>
  <Misuse>
    <ul>
      <li>
        <Markdown>
          To let users select from a list of settings, use [Checkboxes](/components/checkbox)
          instead
        </Markdown>
      </li>
    </ul>
  </Misuse>
</Usage>

## How to use it

### Default

A Toggle’s label is part of its touch target.

import Default from '../../examples/forms/toggle/Default.tsx';
import DefaultCode from '!!raw-loader!../../examples/forms/toggle/Default.tsx';

<CodeExample code={DefaultCode}>
  <Default />
</CodeExample>

### Disabled

A disabled Toggle prevents user interaction. It doesn’t appear in the tab order, can’t receive focus, and may not read
aloud by a screenreader.

import Disabled from '../../examples/forms/toggle/Disabled.tsx';
import DisabledCode from '!!raw-loader!../../examples/forms/toggle/Disabled.tsx';

<CodeExample code={DisabledCode}>
  <Disabled />
</CodeExample>

### Hidden label

Toggle labels can be hidden.

import Hidden from '../../examples/forms/toggle/Hidden.tsx';
import HiddenCode from '!!raw-loader!../../examples/forms/toggle/Hidden.tsx';

<CodeExample code={HiddenCode}>
  <Hidden />
</CodeExample>

### Hint text

Use hint text for additional context.

import Hint from '../../examples/forms/toggle/Hint.tsx';
import HintCode from '!!raw-loader!../../examples/forms/toggle/Hint.tsx';

<CodeExample code={HintCode}>
  <Hint />
</CodeExample>

### Regular weight label

The label is bold by default. It can be made regular weight too.

import RegularWeightLabel from '../../examples/forms/toggle/RegularWeightLabel.tsx';
import RegularWeightLabelCode from '!!raw-loader!../../examples/forms/toggle/RegularWeightLabel.tsx';

<CodeExample code={RegularWeightLabelCode}>
  <RegularWeightLabel />
</CodeExample>

### Validation

Show success, warning, or danger validation messages with the Message component.

import Validation from '../../examples/forms/toggle/Validation.tsx';
import ValidationCode from '!!raw-loader!../../examples/forms/toggle/Validation.tsx';

<CodeExample code={ValidationCode}>
  <Validation />
</CodeExample>

## Configuration

<Configuration reactPackage={props.data.mdx.package} components={props.data.mdx.components} />

## API

The Toggle component follows this structure:

```jsx
<Field>
  <Toggle>
    <Label />
    <Hint />
    <Message />
  </Toggle>
</Field>
```

### Field

<Component components={props.data.mdx.components} componentName="field" />

A Field provides accessibility attributes to its child [Toggle](#toggle) field by associating it
with the corresponding [Label](#label) and [Hint](#hint).

<PropSheet components={props.data.mdx.components} componentName="field" />

### Hint

<Component components={props.data.mdx.components} componentName="hint" />

Nest the Hint within the [Toggle](#toggle) component.

<PropSheet components={props.data.mdx.components} componentName="hint" />

### Label

<Component components={props.data.mdx.components} componentName="label" />

Nest the Label within the [Toggle](#toggle) component.

<PropSheet components={props.data.mdx.components} componentName="label" />

### Message

<Component components={props.data.mdx.components} componentName="message" />

The Message component applies appropriate icon and styles based on the validation provided. Nest
it within a [Field](#field) component.

<PropSheet components={props.data.mdx.components} componentName="message" />

### Toggle

<Component components={props.data.mdx.components} componentName="toggle" />

Nest a Toggle within a [Field](#field) component.

<PropSheet components={props.data.mdx.components} componentName="toggle" />

import { graphql } from 'gatsby';

export const pageQuery = graphql`
  query ($slug: String) {
    ...SidebarPageFragment
  }
`;
