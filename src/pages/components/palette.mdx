---
title: Palette
description: >
  The palette provides the colors used within Garden.
package: '@zendeskgarden/react-theming'
components:
  - theming/src/elements/palette/index.ts
---

import { SEO } from '../../components';

export function Head(props) {
  return <SEO {...props} />;
}

<Usage>
  <Use>

    - To reference brand colors specific to Zendesk products
    - To directly access `hue[shade]` color hex values

  </Use>
  <Misuse>

    - To style custom components with Garden, access the palette through the
    [`theme.palette`](/components/theme-object#palette) API instead
    - To extend Garden with reusable components that respond to theming, use the
    [`getColor`](/components/utilities#getcolor) utility with the
    [`theme.colors`](/components/theme-object#colors) API instead

  </Misuse>
</Usage>

## What it is

### PALETTE

The following JSON displays the palette object in its entirety.

import { PALETTE } from '@zendeskgarden/react-theming';

<ObjectBlock>{PALETTE}</ObjectBlock>

## Configuration

<Configuration reactPackage={props.data.mdx.package} components={[{ name: 'PALETTE' }]} />

import { graphql } from 'gatsby';

export const pageQuery = graphql`
  query ($slug: String) {
    ...SidebarPageFragment
  }
`;
