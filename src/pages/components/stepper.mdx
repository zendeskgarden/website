---
title: Stepper
description: >
  A Stepper guides users through steps of a task in sequential order.
package: '@zendeskgarden/react-accordions'
components:
  - accordions/src/elements/stepper/Stepper.tsx
subcomponents:
  - accordions/src/elements/stepper/components/Content.tsx
  - accordions/src/elements/stepper/components/Label.tsx
  - accordions/src/elements/stepper/components/Step.tsx
---

import { SEO } from '../../components';

export function Head(props) {
  return <SEO {...props} />;
}

<Usage>
  <Use>

    - For multi-step flows that must be completed in order

  </Use>
</Usage>

## How to use it

### Default

The default usage of a Stepper component.

import Default from '../../examples/accordions/stepper/StepperDefault.tsx';
import DefaultCode from '!!raw-loader!../../examples/accordions/stepper/StepperDefault.tsx';

<CodeExample code={DefaultCode}>
  <Default />
</CodeExample>

### Layout

A Stepper is vertical by default but can be horizontal using `isHorizontal`.

import Layout from '../../examples/accordions/stepper/StepperLayout.tsx';
import LayoutCode from '!!raw-loader!../../examples/accordions/stepper/StepperLayout.tsx';

<CodeExample code={LayoutCode}>
  <Layout />
</CodeExample>

### Icons

Label numbers can be replaced with icons.

import Icons from '../../examples/accordions/stepper/StepperIcons.tsx';
import IconsCode from '!!raw-loader!../../examples/accordions/stepper/StepperIcons.tsx';

<CodeExample code={IconsCode}>
  <Icons />
</CodeExample>

## Configuration

<Configuration reactPackage={props.data.mdx.package} components={props.data.mdx.components} />

## API

The Stepper component follows this structure:

```jsx
<Stepper>
  <Stepper.Step>
    <Stepper.Label />
    <Stepper.Content />
  </Stepper.Step>
  {/* other steps */}
</Stepper>
```

{/* markdownlint-disable no-emphasis-as-heading */}

**Stepper orientation**

{/* markdownlint-enable no-emphasis-as-heading */}

- Define the content for a vertical stepper using the `Stepper.Content` for each step
- Render the content for a horizontal stepper externally. See [layout example](#layout) for details.

### Stepper

<Component components={props.data.mdx.components} componentName="stepper" />

<PropSheet components={props.data.mdx.components} componentName="stepper" />

#### Stepper.Content

<Component components={props.data.mdx.subcomponents} componentName="stepper.content" />

Nest the Content within a [Step](#stepperstep) component.

<PropSheet components={props.data.mdx.subcomponents} componentName="stepper.content" />

#### Stepper.Label

<Component components={props.data.mdx.subcomponents} componentName="stepper.label" />

The Label component renders the provided label text with the step number or the provided icon. Nest it
within a [Step](#stepperstep).

<PropSheet components={props.data.mdx.subcomponents} componentName="stepper.label" />

#### Stepper.Step

<Component components={props.data.mdx.subcomponents} componentName="stepper.step" />

Wrap each step's [Label](#stepperlabel) and corresponding [Content](#steppercontent) together with the Step component.

<PropSheet components={props.data.mdx.subcomponents} componentName="stepper.step" />

import { graphql } from 'gatsby';

export const pageQuery = graphql`
  query ($slug: String) {
    ...SidebarPageFragment
  }
`;
