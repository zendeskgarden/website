---
title: Combobox
description: Combobox is an input plus a listbox that appears beneath it. It searches and filters down options, allowing the user to select.
package: '@zendeskgarden/react-dropdowns'
components:
  - 'dropdowns/src/elements/combobox/Combobox.tsx'
  - 'dropdowns/src/elements/combobox/Field.tsx'
  - 'dropdowns/src/elements/combobox/OptGroup.tsx'
  - 'dropdowns/src/elements/combobox/Option.tsx'
  - 'dropdowns/src/elements/combobox/Tag.tsx'
subcomponents:
  - 'dropdowns/src/elements/combobox/Hint.tsx'
  - 'dropdowns/src/elements/combobox/Label.tsx'
  - 'dropdowns/src/elements/combobox/Message.tsx'
  - 'dropdowns/src/elements/combobox/OptionMeta.tsx'
  - 'dropdowns/src/elements/combobox/TagAvatar.tsx'
---

import { SEO } from '../../components';

export function Head(props) {
  return <SEO {...props} />;
}

<Usage>
  <Use>

    - To filter down a large list of options
    - To make a selection from a list of options

  </Use>
  <Misuse>

    - To pick an option from a list on mobile devices, use [Select](/components/select) instead

  </Misuse>
</Usage>

## How to use it

### Default

By default, the Combobox presents a list of suggested options upon value entry.

import Default from '../../examples/combobox/Default.tsx';
import DefaultCode from '!!raw-loader!../../examples/combobox/Default.tsx';

<CodeExample code={DefaultCode}>
  <Default />
</CodeExample>

### Autocomplete

In conjunction with user-provided filtering, the `isAutocomplete` prop
highlights an option for selection.

import Autocomplete from '../../examples/combobox/Autocomplete.tsx';
import AutocompleteCode from '!!raw-loader!../../examples/combobox/Autocomplete.tsx';

<CodeExample code={AutocompleteCode}>
  <Autocomplete />
</CodeExample>

### Disabled

A disabled Combobox prevents user interaction. It doesn't appear in the tab
order, can't receive focus, and may not be read aloud by a screenreader.

import Disabled from '../../examples/combobox/Disabled.tsx';
import DisabledCode from '!!raw-loader!../../examples/combobox/Disabled.tsx';

<CodeExample code={DisabledCode}>
  <Disabled />
</CodeExample>

### Grouped options

Use grouped options for categorization.

import Grouped from '../../examples/combobox/Grouped.tsx';
import GroupedCode from '!!raw-loader!../../examples/combobox/Grouped.tsx';

<CodeExample code={GroupedCode}>
  <Grouped />
</CodeExample>

### Hidden label

Combobox labels can be hidden.

import Hidden from '../../examples/combobox/Hidden.tsx';
import HiddenCode from '!!raw-loader!../../examples/combobox/Hidden.tsx';

<CodeExample code={HiddenCode}>
  <Hidden />
</CodeExample>

### Hint text

Hint text gives further clarification.

import Hint from '../../examples/combobox/Hint.tsx';
import HintCode from '!!raw-loader!../../examples/combobox/Hint.tsx';

<CodeExample code={HintCode}>
  <Hint />
</CodeExample>

### Media

Media elements add even more context to a Combobox and its options.

import Media from '../../examples/combobox/Media.tsx';
import MediaCode from '!!raw-loader!../../examples/combobox/Media.tsx';

<CodeExample code={MediaCode}>
  <Media />
</CodeExample>

### Multiselect

Use the `isMultiselectable` prop to allow multiple selections.

import Multiselect from '../../examples/combobox/Multiselect.tsx';
import MultiselectCode from '!!raw-loader!../../examples/combobox/Multiselect.tsx';

<CodeExample code={MultiselectCode}>
  <Multiselect />
</CodeExample>

### Multiselect tags

Follow Avatar [guidelines](/components/avatar#how-to-use-it-well) by using
Option `tagProps` to alter the appearance of multiselect tags.

import MultiselectTags from '../../examples/combobox/MultiselectTags.tsx';
import MultiselectTagsCode from '!!raw-loader!../../examples/combobox/MultiselectTags.tsx';

<CodeExample code={MultiselectTagsCode}>
  <MultiselectTags />
</CodeExample>

### Nested

A Combobox listbox can contain nested levels for additional categorization of options.

import Nested from '../../examples/combobox/Nested.tsx';
import NestedCode from '!!raw-loader!../../examples/combobox/Nested.tsx';

<CodeExample code={NestedCode}>
  <Nested />
</CodeExample>

### Nested multiselect

A multiselect requires additional consideration for seleting options at nested levels.

import NestedMultiselect from '../../examples/combobox/NestedMultiselect.tsx';
import NestedMultiselectCode from '!!raw-loader!../../examples/combobox/NestedMultiselect.tsx';

<CodeExample code={NestedMultiselectCode}>
  <NestedMultiselect />
</CodeExample>

### Search

Combobox can be used to provide search suggestions.

import Search from '../../examples/combobox/Search.tsx';
import SearchCode from '!!raw-loader!../../examples/combobox/Search.tsx';

<CodeExample code={SearchCode}>
  <Search />
</CodeExample>

### Select only

Add `isEditable={false}` to make Combobox behave like a native `<select>`.

import SelectOnly from '../../examples/combobox/SelectOnly.tsx';
import SelectOnlyCode from '!!raw-loader!../../examples/combobox/SelectOnly.tsx';

<CodeExample code={SelectOnlyCode}>
  <SelectOnly />
</CodeExample>

### Size

Combobox can be default or compact in size.

import Size from '../../examples/combobox/Size.tsx';
import SizeCode from '!!raw-loader!../../examples/combobox/Size.tsx';

<CodeExample code={SizeCode}>
  <Size />
</CodeExample>

### Validation

Show success, warning, and error validation messages with the Message component.

import Validation from '../../examples/combobox/Validation.tsx';
import ValidationCode from '!!raw-loader!../../examples/combobox/Validation.tsx';

<CodeExample code={ValidationCode}>
  <Validation />
</CodeExample>

## How to use it well

### Narrow down options

<BestPractice>
  <Do
    imageSource={props.data.dropdownSelectionDo.childFile.childImageSharp.gatsbyImageData}
    imageIsSquare
  >

    Combobox is useful for lists that are too long to read in full (for example, company
    employees) or that accumulate items over time (for example, an inventory).

  </Do>
</BestPractice>

### Apply icons consistently

<BestPractice>
  <Do
    imageIsFreeForm
    imageSource={props.data.comboboxIconPlacementDo.childFile.childImageSharp.gatsbyImageData}
  >

    Always place the magnifying glass icon at the start of the input when using the Combobox for
    search.

  </Do>
</BestPractice>

## Configuration

<Configuration reactPackage={props.data.mdx.package} components={props.data.mdx.components} />

## API

The Combobox component follows this structure:

```jsx
<Field>
  <Field.Label />
  <Combobox>
    <Option />
    <Option />
    <OptGroup>
      <Option />
    </OptGroup>
    {/* etc. */}
  </Combobox>
</Field>
```

Use `Array.map` to iterate over options.

```jsx
const OPTIONS = [
  { value: 'Sunflower' },
  {
    label: 'More flowers',
    options: [
      { value: 'Violet' },
      { value: 'Daisy' },
    ]
  }
]

<Combobox>
  {OPTIONS.map(option =>
    option.options ? (
      <OptGroup key={option.label} label={option.label}>
        {option.options.map(groupOption => (
          <Option key={groupOption.value} {...groupOption} />
        ))}
      </OptGroup>
    ) : (
      <Option key={option.value} {...option} />
    )
  )}
</Combobox>
```

Match `Option` and `OptGroup` prop signatures when using wrapper components.

```jsx
const OptionComponent = forwardRef((props, ref) => (
  <Option {...props} ref={ref} />
))

const OptGroupComponent = forwardRef((props, ref) => (
  <OptGroup {...props} ref={ref} />
))

<Combobox>
  <OptGroupComponent label="Flowers">
    <OptionComponent value="Violet" />
    <OptionComponent value="Sunflower" />
    <OptionComponent value="Daisy" />
  </OptGroupComponent>
</Combobox>
```

### Combobox

<Component components={props.data.mdx.components} componentName="combobox" />

Nest a Combobox within a [Field](#field) component.

<PropSheet components={props.data.mdx.components} componentName="combobox" />

### Field

<Component components={props.data.mdx.components} componentName="field" />

A Field provides accessibility attributes to the child [Combobox](#combobox) field by associating it
with the corresponding [Field.Label](#fieldlabel) and [Field.Hint](#fieldhint).

<PropSheet components={props.data.mdx.components} componentName="field" />

#### Field.Hint

<Component components={props.data.mdx.subcomponents} componentName="field.hint" />

Nest a Hint within a [Field](#field) component.

<PropSheet components={props.data.mdx.subcomponents} componentName="field.hint" />

#### Field.Label

<Component components={props.data.mdx.subcomponents} componentName="field.label" />

Nest a Label within a [Field](#field) component.

<PropSheet components={props.data.mdx.subcomponents} componentName="field.label" />

#### Field.Message

<Component components={props.data.mdx.subcomponents} componentName="field.message" />

The Message component applies appropriate icon and styles based on the validation provided. Nest
it within a [Field](#field) component.

<PropSheet components={props.data.mdx.subcomponents} componentName="field.message" />

### Option

<Component components={props.data.mdx.components} componentName="option" />

Nest an Option within a [Combobox](#combobox) or [OptGroup](#optgroup) component.

<PropSheet components={props.data.mdx.components} componentName="option" />

#### Option.Meta

<Component components={props.data.mdx.subcomponents} componentName="option.meta" />

Nest Meta within an [Option](#option) component.

<PropSheet components={props.data.mdx.subcomponents} componentName="option.meta" />

### OptGroup

<Component components={props.data.mdx.components} componentName="optgroup" />

Nest an OptGroup within a [Combobox](#combobox) component.

<PropSheet components={props.data.mdx.components} componentName="optgroup" />

### Tag

<Component components={props.data.mdx.components} componentName="tag" />

The Tag is not intended to be used directly. It is used internally by the
Combobox component to render selected options. Reference the props below for
overriding [Option](#option) `tagProps`.

<PropSheet components={props.data.mdx.components} componentName="tag" />

#### Tag.Avatar

<Component components={props.data.mdx.subcomponents} componentName="tag.avatar" />

Use Avatar to render `children` of [Option](#option) `tagProps` with an
associated image.

<PropSheet components={props.data.mdx.subcomponents} componentName="tag.avatar" />

import { graphql } from 'gatsby';

export const pageQuery = graphql`
  query ($slug: String) {
    ...SidebarPageFragment
    dropdownSelectionDo: gardenFigmaAsset(
      name: { eq: "components-dropdowns-autocomplete-selection-do" }
    ) {
      childFile {
        childImageSharp {
          gatsbyImageData(height: 200)
        }
      }
    }
    comboboxIconPlacementDo: gardenFigmaAsset(
      name: { eq: "components-dropdowns-combobox-icon-placement-do" }
    ) {
      childFile {
        childImageSharp {
          gatsbyImageData(height: 200)
        }
      }
    }
  }
`;
