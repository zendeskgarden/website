---
title: Avatar
description: >
  An Avatar is a visual way to represent a person or brand in the product. They can display text,
  icons, or images.
package: '@zendeskgarden/react-avatars'
components:
  - avatars/src/elements/Avatar.tsx
subcomponents:
  - avatars/src/elements/components/Text.tsx
---

<Usage>
  <Use>
    <ul>
      <li>To visually represent a person, brand, or product</li>
    </ul>
  </Use>
</Usage>

## How to use it

### Shape

An Avatar can be a circle or rounded square. See [which shape to use](#how-to-use-it-well).

import AvatarShape from '../../examples/avatar/AvatarShape.tsx';
import AvatarShapeCode from '!!raw-loader!../../examples/avatar/AvatarShape.tsx';

<CodeExample code={AvatarShapeCode}>
  <AvatarShape />
</CodeExample>

### Size

You have 5 sizes to choose from. The default size is `medium`.

import AvatarSize from '../../examples/avatar/AvatarSize.tsx';
import AvatarSizeCode from '!!raw-loader!../../examples/avatar/AvatarSize.tsx';

<CodeExample code={AvatarSizeCode}>
  <AvatarSize />
</CodeExample>

### Status

A colored ring outside the Avatar indicates a user's status. The options are away, available, or active.

import AvatarStatus from '../../examples/avatar/AvatarStatus.tsx';
import AvatarStatusCode from '!!raw-loader!../../examples/avatar/AvatarStatus.tsx';

<CodeExample code={AvatarStatusCode}>
  <AvatarStatus />
</CodeExample>

### Type

An Avatar can contain an icon, an image, or text.

import AvatarType from '../../examples/avatar/AvatarType.tsx';
import AvatarTypeCode from '!!raw-loader!../../examples/avatar/AvatarType.tsx';

<CodeExample code={AvatarTypeCode}>
  <AvatarType />
</CodeExample>

## How to use it well

### Select the right shape for the job

Use circles to represent people and rounded squares for objects, brands, or products. This helps a
user distinguish between the two when scanning a page.

<BestPractice>
  <Do imageSource={props.data.avatarShapeCircle.childFile.childImageSharp.gatsbyImageData}>
    <p>Use a circular Avatar for people.</p>
  </Do>
  <Do imageSource={props.data.avatarShapeSquare.childFile.childImageSharp.gatsbyImageData}>
    <p>Use a rounded square Avatar for objects, brands, or Zendesk products.</p>
  </Do>
</BestPractice>

## Configuration

<Configuration reactPackage={props.data.mdx.package} components={props.data.mdx.components} />

## API

The Avatar component follows this structure:

<!-- prettier-ignore -->
```jsx
<Avatar>
  {/* One of icon, img, or Avatar.Text */}
</Avatar>
```

### Avatar

<Component components={props.data.mdx.components} componentName="avatar" />

<PropSheet components={props.data.mdx.components} componentName="avatar" />

#### Avatar.Text

<Component components={props.data.mdx.subcomponents} componentName="avatar.text" />

<PropSheet components={props.data.mdx.subcomponents} componentName="avatar.text" />

The Text component applies appropriate styles to child text. Nest it within an [Avatar](#avatar) component.
See [type example](#type).

import { graphql } from 'gatsby';

export const pageQuery = graphql`
  query ($fileAbsolutePath: String) {
    ...SidebarPageFragment
    avatarShapeCircle: figmaAsset(name: { eq: "components-avatar-shape-circle" }) {
      childFile {
        childImageSharp {
          gatsbyImageData(height: 200)
        }
      }
    }
    avatarShapeSquare: figmaAsset(name: { eq: "components-avatar-shape-square" }) {
      childFile {
        childImageSharp {
          gatsbyImageData(height: 200)
        }
      }
    }
  }
`;
