---
title: Color picker
description: >
  The Color picker allows users to choose colors using sliders and input fields.
package: '@zendeskgarden/react-colorpickers'
components:
  - colorpickers/src/elements/ColorPicker/index.tsx
  - colorpickers/src/elements/ColorPickerDialog/index.tsx
---

import { SEO } from '../../components';

export function Head(props) {
  return <SEO {...props} />;
}

<Usage>
  <Use>

    - To let users change or update brand colors
    - To let users configure chart colors

  </Use>
</Usage>

## How to use it

### Default

The Color picker allows users to choose colors using sliders and input fields.

import ColorPickerDefault from '../../examples/color-picker/ColorPickerDefault.tsx';
import ColorPickerDefaultCode from '!!raw-loader!../../examples/color-picker/ColorPickerDefault.tsx';

<CodeExample code={ColorPickerDefaultCode}>
  <ColorPickerDefault />
</CodeExample>

### Custom trigger

The Color picker can be displayed using a trigger, e.g. an icon button.

import ColorPickerDialogCustomTrigger from '../../examples/color-picker/ColorPickerDialogCustomTrigger.tsx';
import ColorPickerDialogCustomTriggerCode from '!!raw-loader!../../examples/color-picker/ColorPickerDialogCustomTrigger.tsx';

<CodeExample code={ColorPickerDialogCustomTriggerCode}>
  <ColorPickerDialogCustomTrigger />
</CodeExample>

### Dialog

The Color picker can be shown inside a dialog using `<ColorPickerDialog>`.

import ColorPickerDialogDefault from '../../examples/color-picker/ColorPickerDialogDefault.tsx';
import ColorPickerDialogDefaultCode from '!!raw-loader!../../examples/color-picker/ColorPickerDialogDefault.tsx';

<CodeExample code={ColorPickerDialogDefaultCode}>
  <ColorPickerDialogDefault />
</CodeExample>

### Form input

The Color picker dialog can be used in an input group.

import ColorPickerDialogFormInput from '../../examples/color-picker/ColorPickerDialogFormInput.tsx';
import ColorPickerDialogFormInputCode from '!!raw-loader!../../examples/color-picker/ColorPickerDialogFormInput.tsx';

<CodeExample code={ColorPickerDialogFormInputCode}>
  <ColorPickerDialogFormInput />
</CodeExample>

### Opaque

Remove alpha transparency using the `isOpaque` prop.

import ColorPickerOpaque from '../../examples/color-picker/ColorPickerOpaque.tsx';
import ColorPickerOpaqueCode from '!!raw-loader!../../examples/color-picker/ColorPickerOpaque.tsx';

<CodeExample code={ColorPickerOpaqueCode}>
  <ColorPickerOpaque />
</CodeExample>

## Configuration

<Configuration reactPackage={props.data.mdx.package} components={props.data.mdx.components} />

## API

### ColorPicker

<Component components={props.data.mdx.components} componentName="colorPicker" />

<PropSheet components={props.data.mdx.components} componentName="colorPicker" />

### ColorPickerDialog

<Component components={props.data.mdx.components} componentName="colorPickerDialog" />

<PropSheet components={props.data.mdx.components} componentName="colorPickerDialog" />

import { graphql } from 'gatsby';

export const pageQuery = graphql`
  query ($slug: String) {
    ...SidebarPageFragment
  }
`;
