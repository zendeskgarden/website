---
title: Button
description: >
  Buttons let users take action quickly. Use a Button to enable actions or decisions that are
  important to a user's workflow.
package: '@zendeskgarden/react-buttons'
components:
  - buttons/src/elements/Button.tsx
subcomponents:
  - buttons/src/elements/components/EndIcon.tsx
  - buttons/src/elements/components/StartIcon.tsx
---

import { SEO } from '../../components';

export function Head(props) {
  return <SEO {...props} />;
}

<Usage>
  <Use>

    - To enable user action
    - To draw attention to relevant actions in a user's workflow

  </Use>
  <Misuse>

    - To navigate to another page, use [Anchor](/components/anchor) instead
    - To help users choose from parallel actions, use [Split button](/components/split-button) instead

  </Misuse>
</Usage>

## How to use it

### Default

The typical usage of a Button component.

import ButtonDefault from '../../examples/button/ButtonDefault.tsx';
import ButtonDefaultCode from '!!raw-loader!../../examples/button/ButtonDefault.tsx';

<CodeExample code={ButtonDefaultCode}>
  <ButtonDefault />
</CodeExample>

### Danger

Use danger styling for Buttons that enable destructive action.

import ButtonDanger from '../../examples/button/ButtonDanger.tsx';
import ButtonDangerCode from '!!raw-loader!../../examples/button/ButtonDanger.tsx';

<CodeExample code={ButtonDangerCode}>
  <ButtonDanger />
</CodeExample>

### Disabled

A disabled Button prevents user interaction. It doesn’t appear in the tab order, can’t receive focus, and may not read
aloud by a screenreader.

import ButtonDisabled from '../../examples/button/ButtonDisabled.tsx';
import ButtonDisabledCode from '!!raw-loader!../../examples/button/ButtonDisabled.tsx';

<CodeExample code={ButtonDisabledCode}>
  <ButtonDisabled />
</CodeExample>

### Media

Media elements add even more context to a button.

import ButtonMedia from '../../examples/button/ButtonMedia.tsx';
import ButtonMediaCode from '!!raw-loader!../../examples/button/ButtonMedia.tsx';

<CodeExample code={ButtonMediaCode}>
  <ButtonMedia />
</CodeExample>

### Size

Buttons come in small, medium, and large. The default size is medium.

import ButtonSizes from '../../examples/button/ButtonSizes.tsx';
import ButtonSizesCode from '!!raw-loader!../../examples/button/ButtonSizes.tsx';

<CodeExample code={ButtonSizesCode}>
  <ButtonSizes />
</CodeExample>

### Stretched

Buttons can stretch to fill the width of their container.

import ButtonStretched from '../../examples/button/ButtonStretched.tsx';
import ButtonStretchedCode from '!!raw-loader!../../examples/button/ButtonStretched.tsx';

<CodeExample code={ButtonStretchedCode}>
  <ButtonStretched />
</CodeExample>

### Type

There are 3 types of Buttons: default, primary, and basic.

import ButtonTypes from '../../examples/button/ButtonTypes.tsx';
import ButtonTypesCode from '!!raw-loader!../../examples/button/ButtonTypes.tsx';

<CodeExample code={ButtonTypesCode}>
  <ButtonTypes />
</CodeExample>

## Configuration

<Configuration reactPackage={props.data.mdx.package} components={props.data.mdx.components} />

## API

The Button component follows this structure:

```jsx
<Button>
  <Button.StartIcon /> {/* optional */}
  {/* button text */}
  <Button.EndIcon /> {/* optional */}
</Button>
```

### Button

<Component components={props.data.mdx.components} componentName="button" />

The Button component applies appropriate styles based on its usage and the props provided.

<PropSheet components={props.data.mdx.components} componentName="button" />

#### Button.EndIcon

<Component components={props.data.mdx.subcomponents} componentName="button.endIcon" />

The EndIcon component appropriately positions its child icon within the button. Nest it within the
[Button](#button) component as the last child. See [media example](#media).

<PropSheet components={props.data.mdx.subcomponents} componentName="button.endIcon" />

#### Button.StartIcon

<Component components={props.data.mdx.subcomponents} componentName="button.startIcon" />

The StartIcon component appropriately positions its child icon within the button. Nest it within the
[Button](#button) component as the first child. See [media example](#media).

<PropSheet components={props.data.mdx.subcomponents} componentName="button.startIcon" />

import { graphql } from 'gatsby';

export const pageQuery = graphql`
  query ($slug: String) {
    ...SidebarPageFragment
  }
`;
