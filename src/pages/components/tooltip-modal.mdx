---
title: Tooltip modal
description: >
  A Tooltip modal provides contextual information about a paired element.
  It either opens automatically or through user action.
package: '@zendeskgarden/react-modals'
components:
  - modals/src/elements/TooltipModal/TooltipModal.tsx
---

<Usage>
  <Use>
    <ul>
      <li>To enable user action within a tooltip</li>
      <li>To provide a focus jail when actions need to be inside a tooltip</li>
    </ul>
  </Use>
  <Misuse>
    <ul>
      <li>
        <Markdown>
          To reveal read-only helpful information about an element on hover, use a
          [Tooltip](/components/tooltip)
        </Markdown>
      </li>
    </ul>
  </Misuse>
</Usage>

## How to use it

### Default

A Tooltip Modal contains a header and a way to exit the experience.

import Default from '../../examples/tooltip-modal/TooltipModalDefault.tsx';
import DefaultCode from '!!raw-loader!../../examples/tooltip-modal/TooltipModalDefault.tsx';

<CodeExample code={DefaultCode}>
  <Default />
</CodeExample>

### Placement

A Tooltip modal component can contain a call-to-action, an icon, and
a progress indicator.

There are 13 placement options available. The Tooltip Modal occupies the
`top` position by default. The `auto` placement chooses the position
with the most available space.

import Placement from '../../examples/tooltip-modal/TooltipModalPlacement.tsx';
import PlacementCode from '!!raw-loader!../../examples/tooltip-modal/TooltipModalPlacement.tsx';

<CodeExample code={PlacementCode}>
  <Placement />
</CodeExample>

## Configuration

<Configuration reactPackage={props.data.mdx.package} components={props.data.mdx.components} />

## API

import COMPONENTS from '../../examples/tooltip-modal/components.ts';

### TooltipModal

<Component components={props.data.mdx.components} componentName="tooltipModal" />

<PropSheet components={props.data.mdx.components} componentName="tooltipModal" />

#### TooltipModal.Body

<Component components={COMPONENTS} componentName="tooltipModal.body" />

#### TooltipModal.Close

<Component components={COMPONENTS} componentName="tooltipModal.close" />

#### TooltipModal.Footer

<Component components={COMPONENTS} componentName="tooltipModal.footer" />

#### TooltipModal.FooterItem

<Component components={COMPONENTS} componentName="tooltipModal.footerItem" />

#### TooltipModal.Title

<Component components={COMPONENTS} componentName="tooltipModal.title" />

import { graphql } from 'gatsby';

export const pageQuery = graphql`
  query ($fileAbsolutePath: String) {
    ...SidebarPageFragment
  }
`;
