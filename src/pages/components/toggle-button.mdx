---
title: Toggle button
description: >
  A Toggle Button (like a Toggle) lets users turn something on and off like a
  light switch. Unlike a Checkbox, which is used for selection, a Toggle
  Button is used for activation.
package: '@zendeskgarden/react-buttons'
components:
  - buttons/src/elements/ToggleButton.tsx
---

import { SEO } from '../../components';

export function Head(props) {
  return <SEO {...props} />;
}

<Usage>
  <Use>

    - To see or compare the results of a settings change
    - To activate a mode (such as "dark mode") which takes immediate effect

  </Use>
  <Misuse>

    - To let users select from a list of settings, use [Checkboxes](/components/checkbox) instead

  </Misuse>
</Usage>

## How to use it

### Default

The typical usage of a Toggle Button component.

import ToggleButtonDefault from '../../examples/button/ToggleButtonDefault.tsx';
import ToggleButtonDefaultCode from '!!raw-loader!../../examples/button/ToggleButtonDefault.tsx';

<CodeExample code={ToggleButtonDefaultCode}>
  <ToggleButtonDefault />
</CodeExample>

### Disabled

A disabled Toggle Button prevents user interaction. It doesn’t appear in the
tab order, can’t receive focus, and may not be read aloud by a screenreader.

import ToggleButtonDisabled from '../../examples/button/ToggleButtonDisabled.tsx';
import ToggleButtonDisabledCode from '!!raw-loader!../../examples/button/ToggleButtonDisabled.tsx';

<CodeExample code={ToggleButtonDisabledCode}>
  <ToggleButtonDisabled />
</CodeExample>

### Size

Toggle Buttons can be small, medium, or large. The default size is medium.

import ToggleButtonSizes from '../../examples/button/ToggleButtonSizes.tsx';
import ToggleButtonSizesCode from '!!raw-loader!../../examples/button/ToggleButtonSizes.tsx';

<CodeExample code={ToggleButtonSizesCode}>
  <ToggleButtonSizes />
</CodeExample>

### Type

Toggle Buttons can be default, primary, or basic.

import ToggleButtonTypes from '../../examples/button/ToggleButtonTypes.tsx';
import ToggleButtonTypesCode from '!!raw-loader!../../examples/button/ToggleButtonTypes.tsx';

<CodeExample code={ToggleButtonTypesCode}>
  <ToggleButtonTypes />
</CodeExample>

## Configuration

<Configuration reactPackage={props.data.mdx.package} components={props.data.mdx.components} />

## API

### ToggleButton

<Component components={props.data.mdx.components} componentName="toggleButton" />

<PropSheet components={props.data.mdx.components} componentName="toggleButton" />

import { graphql } from 'gatsby';

export const pageQuery = graphql`
  query ($slug: String) {
    ...SidebarPageFragment
  }
`;
